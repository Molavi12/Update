name: Run Telegram Bot

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6 ساعت زمان اجرا
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Create session file from secret
      run: |
        echo "${{ secrets.TELEGRAM_SESSION }}" > session_name.session
        echo "✅ Session file created from secret"
        
    - name: List files (debug)
      run: ls -la
      
    - name: Run Telegram Bot
      run: |
        # بررسی وجود فایل‌ها و اجرای صحیح
        if [ -f "simple-bot.py" ]; then
          echo "🔍 پیدا شد: simple-bot.py"
          python simple-bot.py
        elif [ -f "bot.py" ]; then
          echo "🔍 پیدا شد: bot.py" 
          python bot.py
        else
          echo "❌ هیچ فایل رباتی پیدا نشد!"
          echo "📁 فایل‌های موجود:"
          ls -la
          exit 1
        fi
      env:
        PYTHONUNBUFFERED: 1
        
    - name: Upload logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: bot-logs
        path: |
          *.log
          session_name.session
          simple-bot.py
          bot.py
        retention-days: 1
